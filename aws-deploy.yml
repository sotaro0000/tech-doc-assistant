# AWS ECS/Fargate デプロイ設定

# 前提条件:
# 1. AWS CLIインストール
# 2. ECRリポジトリ作成
# 3. ECSクラスター作成
# 4. RDS PostgreSQL作成
# 5. VPC/サブネット/セキュリティグループ設定

# コマンド例:
# aws ecr create-repository --repository-name tech-doc-backend --region ap-northeast-1
# aws ecr create-repository --repository-name tech-doc-frontend --region ap-northeast-1

# aws ecs create-cluster --cluster-name tech-doc-cluster --region ap-northeast-1

# aws rds create-db-instance \
#   --db-instance-identifier tech-doc-postgres \
#   --db-instance-class db.t3.micro \
#   --engine postgres \
#   --engine-version 15.4 \
#   --master-username dbadmin \
#   --master-user-password <your-password> \
#   --allocated-storage 20 \
#   --region ap-northeast-1